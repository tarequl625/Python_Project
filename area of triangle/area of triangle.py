# Calculate area of a triangle

height = float(input("Enter height value: "))   # Example input: 10
base = float(input("Enter base value: "))       # Example input: 5

# Formula: (1/2) × base × height
area = (1/2) * height * base

print("Area is:", area)  
# Output: Area is: 25.0